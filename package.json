{
  "name": "if-exist",
  "version": "0.0.2",
  "description": "Check if file or directory exists",
  "license": "MIT",
  "bin": "dist/cli.js",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/igrek8/if-exist.git"
  },
  "bugs": {
    "url": "https://github.com/igrek8/if-exist/issues"
  },
  "dependencies": {
    "commander": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.14.0",
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "clean": "rimraf ./dist",
    "build:ts": "tsc",
    "build": "npm run clean && npm run build:ts"
  }
}
